<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @controller_name.camelize %>Controller - <%= @config.app_name %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  <%= render partial: 'rails_doc_generator/docs/styles' %>
</head>
<body>
  <header class="header">
    <div class="container">
      <div class="header-content">
        <div>
          <h1><%= @config.app_name %></h1>
          <p class="subtitle">API Documentation</p>
        </div>
      </div>
    </div>
  </header>

  <main class="container">
    <div class="breadcrumb">
      <%= link_to "Home", rails_doc_generator.root_path %>
      <span class="breadcrumb-separator">/</span>
      <%= link_to "Routes", rails_doc_generator.routes_path %>
      <span class="breadcrumb-separator">/</span>
      <span class="breadcrumb-current"><%= @controller_name.camelize %></span>
    </div>

    <div class="stats-grid" style="grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); margin-bottom: 2rem;">
      <div class="stat-card">
        <div class="stat-value"><%= @data[:routes].size %></div>
        <div class="stat-label">Routes</div>
      </div>
      <div class="stat-card">
        <div class="stat-value"><%= @data[:actions].size %></div>
        <div class="stat-label">Actions</div>
      </div>
      <div class="stat-card" style="text-align: left; padding: 1.25rem;">
        <div class="stat-label" style="margin-bottom: 0.5rem;">Base Path</div>
        <code style="font-size: 1rem;"><%= @data[:base_path] || '/' %></code>
      </div>
    </div>

    <section class="section">
      <h2 class="section-header">Actions</h2>
      <div style="display: flex; flex-wrap: wrap; gap: 0.5rem; margin-bottom: 2rem;">
        <% @data[:actions].each do |action| %>
          <span class="badge badge-count" style="font-size: 0.8125rem; padding: 0.375rem 0.75rem;"><%= action %></span>
        <% end %>
      </div>
    </section>

    <section class="section">
      <h2 class="section-header">Routes</h2>
      <% if @data[:routes].any? %>
        <div class="table-container">
          <table>
            <thead>
              <tr>
                <th style="width: 100px;">Method</th>
                <th>Path</th>
                <th>Action</th>
                <th>Route Name</th>
                <th>Constraints</th>
              </tr>
            </thead>
            <tbody>
              <% @data[:routes].each do |route| %>
                <tr>
                  <td>
                    <span class="badge badge-<%= route.verb.to_s.downcase.split('|').first %>">
                      <%= route.verb %>
                    </span>
                  </td>
                  <td><code><%= route.path %></code></td>
                  <td><code><%= route.action %></code></td>
                  <td>
                    <% if route.name %>
                      <code><%= route.name %></code>
                    <% else %>
                      <span style="color: var(--text-dim);">—</span>
                    <% end %>
                  </td>
                  <td>
                    <% if route.constraints.any? %>
                      <% route.constraints.each do |key, value| %>
                        <code><%= key %>: <%= value %></code><br>
                      <% end %>
                    <% else %>
                      <span style="color: var(--text-dim);">—</span>
                    <% end %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% else %>
        <div class="empty-state">
          <div class="empty-state-icon">🛣️</div>
          <p>No routes found</p>
        </div>
      <% end %>
    </section>
  </main>

  <footer class="footer">
    <div>Generated by <a href="https://github.com/ArshdeepGrover/rails_doc_generator" target="_blank">RailsDocGenerator</a></div>
    <div style="margin-top: 0.5rem;">Developed & maintained with ❤️ by <a href="https://www.arshdeepsingh.info?utm_source=rails_doc_generator&utm_medium=footer&utm_campaign=developer_credit&utm_content=docs_controller" target="_blank">Arshdeep Singh</a></div>
  </footer>
</body>
</html>
